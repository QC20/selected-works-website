
export interface MusicProjectsProps {}

const MusicProjects: React.FC<MusicProjectsProps> = () => {
    const [startSimulation, setStartSimulation] = useState(false);
    const [currentSong, setCurrentSong] = useState('');
    
    const handleStartSimulation = () => {
        setStartSimulation(true);
    };



            {!startSimulation && (
                <div style={{ textAlign: 'center', margin: '20px' }}>
                    <button onClick={handleStartSimulation}>Start Game of Life Simulation</button>
                </div>
            )}

            {/* The iframe for the simulation */}
            {startSimulation && (
                <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', margin: '20px' }}>
                    <iframe src="https://qc20.github.io/game-of-life-simulation/" style={{ width: '800px', height: '600px', border: 'none' }} title="Game of Life Simulation"></iframe>
                </div>
            )}
